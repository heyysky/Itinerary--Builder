🧳 Travel Itinerary Planner
Your AI Travel Buddy — Personalized, Instant, and Smart

The Travel Itinerary Planner is an AI-powered application that helps users plan personalized day-trip itineraries for any city in the world — based on their interests, like food, history, art, nightlife, etc.

It leverages LangChain, LangGraph, and Groq’s Llama 3.1 model, combined with Gradio’s interactive UI, to make travel planning feel effortless and conversational.

🌟 Overview

Ever wished you could tell an AI your mood and have it plan a day for you?
This app does exactly that.

You simply enter:

A city name 🏙️

A list of interests (e.g., “food, culture, museums”) 🎨

And the system generates a personalized hour-by-hour itinerary including:

Morning, afternoon, and evening plans

Local spots matching your interests

Activity balance between sightseeing, rest, and fun

All generated by an LLM (Large Language Model) fine-tuned for creative and structured travel responses.

🧠 How It Works (Step-by-Step Architecture)
User Input → LangGraph Workflow → ChatPromptTemplate → Groq LLM → Gradio Output

1️⃣ Input Stage

The user provides:

City → (e.g., “Paris”)

Interests → (e.g., “art, history, coffee shops”)

This data initializes the planner’s state graph.

2️⃣ Workflow (LangGraph Orchestration)

The project uses a LangGraph StateGraph to control conversation flow.

Each node represents a state:

input_city — Captures the city

input_interest — Captures interests

create_itinerary — Generates the plan

Edges define transitions between these states until the END node is reached.

3️⃣ Prompting Layer

The ChatPromptTemplate defines how the model is instructed:

("system", "You are a helpful travel assistant. Create a day trip itinerary for {city} based on the user's interests: {interests}. Provide a brief, bulleted itinerary.")
("human", "Create an itinerary for my day trip.")


This ensures the model generates structured, readable itineraries.

4️⃣ LLM Execution

The Groq Llama 3.1 8B-Instant model processes the final prompt.
This model is:

Optimized for low-latency, high-quality outputs

Excellent at structured text generation

Backed by the Groq API for fast inference

5️⃣ Gradio Interface

A beautiful, interactive UI is built with Gradio:

Users enter their city and interests

The output area displays the generated itinerary

Supports any city worldwide

interface = gr.Interface(
    fn=travel_planner,
    theme='Yntec/HaleyCH_Theme_Orange_Green',
    inputs=[
        gr.Textbox(label="Enter the city for your day trip"),
        gr.Textbox(label="Enter your interests (comma-separated)")
    ],
    outputs=gr.Textbox(label="Generated Itinerary"),
    title="Travel Itinerary Planner",
    description="Enter a city and your interests to generate a personalized day trip itinerary."
)

🏗️ System Design Diagram
 ┌────────────────────────┐
 │      User Input        │
 │  (City + Interests)    │
 └──────────┬─────────────┘
            │
            ▼
 ┌────────────────────────┐
 │ LangGraph State Machine│
 │  • input_city          │
 │  • input_interest      │
 │  • create_itinerary    │
 └──────────┬─────────────┘
            │
            ▼
 ┌────────────────────────┐
 │  LangChain + ChatPrompt│
 │  Generates structured  │
 │  itinerary prompt       │
 └──────────┬─────────────┘
            │
            ▼
 ┌────────────────────────┐
 │  Groq Llama 3.1 Model  │
 │  Returns final text     │
 └──────────┬─────────────┘
            │
            ▼
 ┌────────────────────────┐
 │  Gradio Web Interface  │
 │  Displays itinerary     │
 └────────────────────────┘

🧩 Technologies Used
Tech	Purpose
Python 3.10+	Core programming language
LangChain	LLM pipeline + prompt management
LangGraph	Conversational flow control (state graph)
Groq API	Fast inference using Llama 3.1
Gradio	Frontend UI for input/output
Typing / Annotated	Type-safe state definitions
📂 Project Structure
travel_itinerary_planner/
│
├── main.py                  # Gradio + main logic
├── planner_state.py         # TypedDict for PlannerState
├── workflow.py              # LangGraph workflow nodes
├── prompts.py               # LLM prompt templates
├── utils/                   # Helper scripts (optional)
├── requirements.txt          # Dependencies
└── README.md                # Documentation

🧾 Example Run
🔹 Input
City: Rome
Interests: art, history, food

🔹 Output
☀️ Morning: Visit the Colosseum and Roman Forum  
🍝 Lunch: Enjoy pasta at Trattoria Da Enzo in Trastevere  
🎨 Afternoon: Explore Vatican Museums and Sistine Chapel  
🌆 Evening: Walk around Piazza Navona and enjoy gelato by the fountain  

⚙️ Setup Guide
Step 1: Clone the Repo
git clone https://github.com/<your-username>/travel-itinerary-planner.git
cd travel-itinerary-planner

Step 2: Install Dependencies
pip install -r requirements.txt

Step 3: Set Your Groq API Key

Create a .env file or export directly:

export GROQ_API_KEY="your_api_key_here"

Step 4: Run the App
python main.py


Then open the Gradio link displayed in your terminal (usually http://127.0.0.1:7860).

🧰 Requirements

Add this to your requirements.txt:

gradio
langchain
langgraph
langchain-groq
typing-extensions

🧭 Future Enhancements

✅ Multi-day itinerary generation
✅ Integration with Google Maps API
✅ Real-time weather updates
✅ Save itineraries as downloadable PDFs
✅ Voice input for hands-free planning
